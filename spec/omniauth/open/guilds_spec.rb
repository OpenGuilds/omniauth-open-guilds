RSpec.describe Omniauth::Open::Guilds do
  it "has a version number" do
    expect(Omniauth::Open::Guilds::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
